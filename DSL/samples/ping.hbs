{{!--
  Sample: liveness probe with server-generated timestamp.

  curl -sS -X POST http://localhost:3000/samples/ping \
    -H 'Content-Type: application/json' \
    -H 'type: json' \
    -d '{}'
--}}
{ "service": "DataMapper", "project": "samples", "ok": true, "ts": "{{now}}" }
